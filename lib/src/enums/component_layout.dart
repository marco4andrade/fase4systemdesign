/// Enum general para orientaciones de componentes
/// Puede ser aplicado a dividers, layouts, etc.
enum Pragma4ComponentOrientation {
  horizontal,
  vertical,
}

/// Enum general para posiciones de elementos
/// Puede ser aplicado a iconos, labels, etc.
enum Pragma4ComponentPosition {
  leading,
  trailing,
  top,
  bottom,
  center,
  start,
  end,
}

/// Enum general para alineaciones
enum Pragma4ComponentAlignment {
  start,
  center,
  end,
  spaceBetween,
  spaceAround,
  spaceEvenly,
}

/// Enum para tipos de texto/tipografía
enum Pragma4TextType {
  displayLarge,
  displayMedium,
  displaySmall,
  headlineLarge,
  headlineMedium,
  headlineSmall,
  titleLarge,
  titleMedium,
  titleSmall,
  labelLarge,
  labelMedium,
  labelSmall,
  bodyLarge,
  bodyMedium,
  bodySmall,
}